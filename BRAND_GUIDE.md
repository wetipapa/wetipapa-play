# WTPP PLAY 브랜드 가이드 — 웹 구현 기준

> **상위 문서가 있다.** 브랜드·캐릭터의 기준은 `Wetipapa/brand-assets/BRAND_GUIDE.md`이고,
> 이 문서는 그것을 화면에 구현하는 층을 다룬다. 충돌하면 상위 문서가 이긴다.
> 이 문서는 상위 문서를 부정할 수 없고, 더 좁고 구체적으로 정할 수만 있다.

| 층 | 문서 | 무엇을 정하나 |
|---|---|---|
| 상위 | `brand-assets/BRAND_GUIDE.md` | 캐릭터 캐논, CI, 브랜드 컬러, 자산 레지스트리 |
| 하위 | **이 문서** | CSS 토큰, 서체, 로고 컴포넌트, 대비·접근성, 새 서비스 부트스트랩 |

이 문서는 `웨티아빠 / WETIPAPA`가 만드는 모든 어린이 학습 서비스가 공유하는
**웹 구현 기준**이다. 이 저장소(`wetipapa-play`)의 `/brand` 폴더가
**코드 자산(토큰·서체·컴포넌트)의 기준 원본(source of truth)**이며,
새 서비스를 만들 때는 이 폴더를 그대로 복사해 시작한다.

**캐릭터 그림·CI·심벌의 원본은 여기가 아니라 `brand-assets/confirmed/`다.**
웹앱 밖(블로그·인쇄·SNS)에서도 쓰는 자산이라 특정 서비스 저장소에 둘 수 없다.
쓸 그림은 거기서 이 저장소로 **복사해서** 커밋한다.

```
brand/
  tokens.css              브랜드 컬러·폰트·타이포·형태 디자인 토큰 (CSS 커스텀 프로퍼티)
  assets/
    mascot-hero.png        원본 캐릭터를 여백만 정리한 히어로용 마스코트 (투명 배경)
    mascot-icon-master.png 파비콘/앱 아이콘 제작에 쓰인 1024x1024 마스터
BRAND_GUIDE.md             (이 문서, 저장소 루트)
```

실제로 배포되는 파비콘류(`app/icon.png`, `app/apple-icon.png`, `app/favicon.ico`,
`app/opengraph-image.png`, `public/icon-192.png`, `public/icon-512.png`)는
`scripts/generate-brand-assets.mjs`가 `/brand/assets`의 원본으로부터 1회성으로
생성한 **파생 산출물**이다. 즉 "원본은 `/brand`, 배포용 파일은 생성물"이라는
역할 분리를 지킨다.

---

## 1. 브랜드 구조와 명칭

```text
WTPP                   ← 대표 브랜드 (헤더, 파비콘, OG, 푸터)
├─ 웨티아빠 / WETIPAPA  ← 콘텐츠 시리즈 (블로그, Threads, 화자·서명)
└─ WTPP PLAY           ← 어린이 학습 서비스 통합 허브 (이 저장소)
   ├─ WTPP VOCA        웨티 보카 — 영어 단어장 + 테스트
   ├─ WTPP RACING      웨티 레이싱 — 구구단 레이싱 게임
   ├─ WTPP CLOCK       웨티 시계탐험대 — 시계 읽기 게임
   ├─ WTPP POP         웨티 구구단 팡팡 — 풍선 터뜨리기 게임
   └─ (향후 추가될 학습 도구·게임)
```

- **`by 웨티아빠` 표기는 쓰지 않는다.** 대표 브랜드가 WTPP로 바뀌면서
  "서비스가 웨티아빠에 딸려 있다"는 관계 자체가 맞지 않게 됐다.
  창작자를 드러내는 일은 푸터의 `웨티아빠 채널`(블로그·Threads) 링크가 맡는다.
- 대표 CI 자리(헤더 로고, 파비콘, 앱 아이콘, OG, 푸터)에는 `WTPP` 마스터 심벌을 쓴다.
- 아이가 읽는 화면 제목에는 한글 서비스명(`웨티 보카`)을 쓴다. 주 사용자가 7세다.
- `WTPP VOCA` 같은 영문 서비스명은 저장소·문서·제휴 자료에서 서비스를 가리킬 때 쓴다.

## 2. 로고 사용법

WTPP PLAY는 캐릭터를 다시 그리지 않고, **확정 WTPP 마스터 심벌
(`brand-assets/confirmed/ci/wtpp_symbol_color.png`) + 텍스트 워드마크**로 로고를 구성한다
(`components/Logo.tsx`가 실제 구현이자 참조 예제).

### 기본 가로형 로고
- 구성: WTPP 마스터 심벌 + `WTPP`(디스플레이 서체) + `PLAY`(한 단계 작은 서비스 표기)
- 심벌이 이미 네이비 라운드 타일을 갖고 있어 배경을 따로 깔지 않는다
- 최소 크기: 심벌 20px 이상에서 사용할 것 (그 이하는 두 얼굴이 뭉개짐)
- 주변 여백(clear space): 심벌 너비의 8% 이상 (상위 가이드 §7)

### 축약형 브랜드 마크 (compact)
- 좁은 공간(모바일 Footer 등)에서는 서비스 표기를 생략하고
  심벌 + `WTPP` 워드마크만 사용한다.
- `<Logo variant="compact" />`

### 단색 버전 (mono)
- 인쇄, 워터마크 등 단색이 필요한 곳에서는 배지를 아웃라인으로, 텍스트를
  `currentColor`로 렌더링한다.
- `<Logo variant="mono" />`

### 밝은/어두운 배경 대응
- 로고 텍스트는 `currentColor`를 상속하므로, 어두운 배경 위에서는 감싸는
  요소에 밝은 글자색(`#F4ECE4` 등)만 지정하면 자동으로 대응된다
  (`components/Footer.module.css`의 `.footerLogo` 참고).
- 배지(반짝임 아이콘)는 배경 밝기와 무관하게 `--wp-color-primary-bright`
  고정 배경 + 흰색 아이콘을 유지해 항상 또렷하게 보이도록 한다.

### 파비콘 / 앱 아이콘
- 공식 컬러 심벌(네이비 라운드 타일 + 아빠·웨티)을 정사각형 흰 배경 위에 담았다.
- 심벌 자체가 이미 라운드 타일이라 별도 프레임을 두르지 않는다.
- 캐릭터를 새로 그리거나 얼굴을 잘라내지 않고, 원본 구도를 그대로 축소했다.
- 생성 스크립트: `scripts/generate-brand-assets.mjs`

## 3. 컬러

모든 조합은 WCAG AA(본문 텍스트 4.5:1 이상) 기준으로 검증했다. 값은
`brand/tokens.css`가 원본이며, 여기 표는 참고용 요약이다.

### 브랜드 색과의 관계

화면 토큰은 브랜드 아이덴티티 색을 **버린 것이 아니라, 명도만 조정해 파생한 것**이다.
색상(hue)은 그대로다.

| 브랜드 색 (상위 문서 §5) | 화면 토큰 | 관계 |
|---|---|---|
| 옐로 `#FD9F02` (H 37.5°) | `--wp-color-primary-bright` `#F0900C` (H 34.7°) | **같은 주황.** 색상각 2.8° 차이 |
| 옐로 `#FD9F02` | `--wp-color-primary` `#B04A0A` | 글자 대비용으로 어둡게 (5.48:1) |
| 옐로 `#FD9F02` | `--wp-color-bg` `#FFF7EA` (H 37.1°) | 같은 색상의 아주 밝은 톤 |
| 블랙 `#111111` | `--wp-color-ink` `#33241C` | 완전 검정 대신 따뜻한 잉크 |
| 네이비 `#013075` | **없음** | 도입 예정 (아래) |
| 코랄 `#FD4739` | **쓰지 않음** | 웨티 상의 색. UI에 쓰면 캐릭터와 섞인다 |

브랜드 옐로 `#FD9F02`를 글자나 버튼 라벨에 그대로 쓰지 않는다.
흰 배경 위 대비가 `2.07:1`이라 읽히지 않는다.

### 네이비 (2026-08-14 도입)

브랜드 주색 1번인 네이비 `#013075`. 대비가 흰 배경 `12.49:1`, 크림 배경 `11.75:1`로
화면에서 가장 안전한 색이다. 공식·신뢰의 자리에만 쓴다.

| 토큰 | HEX | 쓰는 자리 |
| --- | --- | --- |
| `--wp-color-navy` | `#013075` | 푸터 배경, 포커스 링, 공식 관계 표기 |
| `--wp-color-navy-bright` | `#1C4C9C` | 네이비 위 보조 강조 (흰 배경 8.19:1) |
| `--wp-color-navy-tint` | `#DDE6F5` | 연한 네이비 배경 (배지) |
| `--wp-color-navy-ink` | `#012354` | `navy-tint` 위 글자 (12.20:1) |

적용된 곳: 푸터 배경(`Footer.module.css`), 포커스 링(`--wp-focus-ring`),
브랜드 소개 영역(`BrandIntro.module.css`), 헤더 하단 경계.

푸터 대비 검증: 본문 글자 `10.69:1`, 보조 글자 `5.28:1`, 오렌지 링크 hover `5.18:1`.

**`--wp-color-ink`를 네이비로 바꾸지 않는다** — 크림 배경과 함께 차가워져
7세 대상의 따뜻한 인상이 깨진다.

| 토큰 | HEX | 용도 |
| --- | --- | --- |
| `--wp-color-bg` | `#FFF7EA` | 페이지 배경 (따뜻한 크림톤) |
| `--wp-color-surface` | `#FFFFFF` | 카드/패널 배경 |
| `--wp-color-ink` | `#33241C` | 기본 글자색 |
| `--wp-color-ink-soft` | `#6B5B52` | 보조 설명 글자색 |
| `--wp-color-primary` | `#B04A0A` | 버튼/링크 글자 대비용 (딥 오렌지) |
| `--wp-color-primary-bright` | `#F0900C` | 장식·배지·아이콘 (비비드 오렌지) |

### 서비스 확장 컬러
| 서비스 | 포인트 컬러 | 톤 배경 | 용도 |
| --- | --- | --- | --- |
| 웨티 보카 | `#2670C4` (블루) | `#DCEBFA` | 차분하고 신뢰감 있는 인상 |
| 웨티 레이싱 | `#C23C0C` (레드오렌지) | `#FBE2D5` | 속도감, 에너지 |
| 웨티 시계탐험대 | `#7C4FE0` (바이올렛) | `#E8DFFB` | 호기심, 탐험 |

새 서비스를 추가할 때는 위 세 색과 겹치지 않는 새로운 색상을 골라
`brand/tokens.css`에 `--wp-color-{key}` / `-tint` / `-ink` 세 변수를
추가한다. **반드시 흰 배경 대비 4.5:1 이상**을 만족하는 값으로 정할 것
(간단한 대비 계산은 브라우저 개발자 도구나 온라인 WCAG contrast checker 사용).

## 4. 타이포그래피

- **디스플레이(제목/로고/버튼)**: `Jua` — 둥글고 통통한 단일 굵기 한글 서체.
  아이 눈높이의 친근한 인상을 준다. 제목, 로고 워드마크, CTA 버튼에만
  제한적으로 사용한다 (본문에 쓰면 가독성이 떨어짐).
- **본문(설명/부모용 텍스트)**: `Noto Sans KR` (400/500/700) — 신뢰감 있고
  작은 크기에서도 읽기 쉬운 서체.
- 두 서체 모두 `next/font/google`로 자체 호스팅되어 외부 네트워크 요청이나
  로딩 지연(FOUT) 없이 안정적으로 렌더링된다.
- 한글 텍스트는 항상 `word-break: keep-all`을 유지해 단어 중간이
  잘려 줄바꿈되지 않도록 한다 (`app/globals.css`에서 전역 적용).

## 5. 디자인 토큰 (형태)

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--wp-radius-sm` / `md` / `lg` / `pill` | 12 / 20 / 28 / 999px | 버튼·카드·배지 모서리 |
| `--wp-shadow-card` / `-hover` | — | 카드 기본/호버 그림자 |
| `--wp-focus-ring` | 흰 후광 3px + 잉크색 외곽선 6px | 키보드 포커스 표시 (모든 배경에서 보임) |
| `--wp-space-xs ~ xl` | 0.5rem ~ 4rem | 여백 스케일 |

## 6. 마스코트 사용 원칙

> **전환 완료 (2026-08-14).** 이전에는 OGQ 리액션 스티커(`04_approved`)를
> 마스코트·파비콘으로 썼으나, 상위 문서 §4.3·§6.1이 "공식 표식 자리에 리액션 캐릭터를
> 단독으로 쓰지 않는다"로 정했다. 스티커는 문구가 붙은 콘텐츠형 자산이라 공식 표식에 맞지 않고,
> 캡션 밴드를 잘라내면 원본 구도를 훼손하게 된다.
>
> | 자리 | 무엇을 쓰나 |
> |---|---|
> | 파비콘·앱 아이콘 | 컬러 심벌 `wetipapa_symbol_color_1024.png` |
> | 히어로 (브랜드 소개 영역) | 아빠+웨티 합본 컷 `wetipapa_weti_together_01.png` |
> | 성공·실패 메시지 | 흑백 리액션 캐릭터 (기존 유지) |
>
> 두 원본은 `brand/source/`에 사본으로 두고, `scripts/generate-brand-assets.mjs`가
> 여기서 파비콘·아이콘·히어로·OG를 생성한다.

- 현재 원본: `brand/source/` (상위 `brand-assets/confirmed/`에서 복사).
  OGQ 세트(`wetipapa_ogq_submission_v10/`)는 리액션 콘텐츠용으로 그대로 보존한다.
- 얼굴, 손, 반짝임 등 캐릭터의 의미 있는 부분을 잘라내거나 비율을
  왜곡하지 않는다. 필요하면 **캐릭터 주변 여백만** 정리한다.
- 마스코트는 페이지에서 **의미 있는 자리 한 곳(브랜드 소개 영역)에 크게**
  사용하고, 장식 목적으로 반복 배치하지 않는다. 파비콘/앱 아이콘처럼
  기능적으로 다른 용도일 때는 별도 크롭을 허용한다(§2 참고).
- 마스코트 이미지에는 항상 실제 동작을 설명하는 대체 텍스트를 제공한다
  (예: "엄지를 척 들며 반갑게 인사하는 웨티아빠 캐릭터"). 순수 장식
  요소(반짝임 등 배경 장식)는 `aria-hidden="true"`로 접근성 트리에서 제외한다.
- 다른 포즈의 스티커(`wetipapa_ogq_submission_v10/stickers/`)를 향후
  이벤트 배너 등에 쓰고 싶다면, 캡션 텍스트 밴드만 제외하고 캐릭터
  전체를 그대로 사용한다 (`scripts/generate-brand-assets.mjs`의 크롭
  로직 참고).

## 7. 새 서비스에 브랜드 적용하기

1. 이 저장소의 `brand/` 폴더(및 이 문서)를 새 서비스 저장소로 복사한다.
2. `brand/tokens.css`를 그대로 새 프로젝트의 전역 스타일에 import한다.
3. 로고는 `components/Logo.tsx`의 구조(WTPP 심벌 + 워드마크)를 그대로
   재현한다 — 새 이미지를 만들 필요 없이, 같은 심벌과
   `WTPP`/서비스명 텍스트 조합을 재사용한다.
4. 파비콘이 필요하면 `scripts/generate-brand-assets.mjs`를 참고해 같은
   방식(원본 캐릭터 + 브랜드 컬러 프레임)으로 생성한다.
5. 서비스 고유 포인트 컬러가 필요하면 §3의 규칙대로 새 컬러 세트를
   `tokens.css`에 추가한다.
6. Footer에는 WTPP 로고와 `웨티아빠 채널`(블로그·Threads) 링크를 남긴다.

## 8. 하면 안 되는 예시

- ❌ 마스코트를 새로 그리거나 다른 캐릭터로 대체하기
- ❌ 마스코트의 얼굴, 손, 반짝임 등 일부를 잘라내며 전체 구도를 바꾸기
- ❌ 원본 이미지의 가로세로 비율을 강제로 늘리거나 줄이기
- ❌ 마스코트를 페이지 전체에 배경 패턴처럼 반복 배치하기
- ❌ 대비 검증 없이 임의의 밝은 색을 버튼 배경 + 흰 글자 조합으로 사용하기
- ❌ `Jua` 서체를 긴 본문 문단에 사용하기 (가독성 저하)
- ❌ 서비스마다 완전히 다른 톤앤매너(폰트/모서리 반경/그림자)를 새로 정의하기
  — 포인트 컬러만 바꾸고 나머지 토큰은 공유해야 "하나의 WTPP"로 보인다

## 9. 자산 관리 원칙 요약

| 무엇 | 어디 | 성격 |
| --- | --- | --- |
| **브랜드·캐릭터 기준 문서** | `Wetipapa/brand-assets/BRAND_GUIDE.md` | **상위 문서.** 충돌 시 우선 |
| **캐릭터 그림·CI·심벌 원본** | `Wetipapa/brand-assets/confirmed/` | **기준 원본.** 여기로 복사해서 쓴다 |
| 이 저장소가 쓰는 원본 사본 | `brand/source/` | 위에서 복사해 온 것. 생성 스크립트의 입력 |
| 원본 캐릭터 그림 (사본) | `wetipapa_ogq_submission_v10/` | 절대 수정 금지, 그대로 보존 |
| 이 저장소의 마스코트·파비콘 원본 | `brand/assets/` | 위 원본에서 파생한 이 저장소용 사본 |
| 컬러·폰트·디자인 토큰 | `brand/tokens.css` | 기준 원본 |
| 브랜드 사용 기준 | `BRAND_GUIDE.md` (이 문서) | 기준 문서 |
| 실제 배포용 파비콘/OG 이미지 | `app/icon.png` 등 | 생성된 산출물 (원본이 아님) |
